---
description: 作品集图片资源处理规则：禁止擅自转格式、禁止用聊天压缩图顶替高清导出
alwaysApply: true
---

# 图片资源处理（强制）

## 禁止擅自改格式

- 用户提供 **PNG** 时，站点必须继续用 **PNG**，禁止转成 JPEG/JPG「为了体积」或「对齐旧文件名」。
- 用户提供 **JPG** 时，保持 JPG，除非用户明确要求转换。
- 需要改扩展名时，同步改 `portfolio.js` / `makeSlidesFromPages(..., ext)` 的 `ext`，不要只改磁盘文件。

## 清晰度：只用真源文件

- **聊天里拖入的图片不可靠**：Cursor 附件常被压成小图，且可能变成「后缀 .png、内容实为 JPEG」。不能当作设计稿 2× 导出使用。
- 替换方案/页面大图时，优先从用户本机路径拷贝真文件，例如桌面的 `方案设计1.png`～`方案设计6.png`（Figma 2× PNG）。
- 用 `cp` 原样拷贝；禁止先缩小再放大，禁止为「统一成 jpg」重编码。
- 接入前用文件头校验：`\x89PNG` = 真 PNG，`\xff\xd8\xff` = 实为 JPEG。尺寸应接近导出标注（2× 常为数千像素宽），若只有几百～1024px，说明拿到的是预览图，必须停下来向用户要原文件路径。

## 替换流程（项目一方案图示例）

1. 确认源文件：真 PNG + 足够分辨率（相对显示宽约 1152，源宽建议 ≥2300，2× 导出通常远大于此）。
2. 写入 `public/images/pages/page-XX.png`（或用户指定路径），保持 PNG。
3. `makeSlidesFromPages([...], ..., 'png')`，并加缓存参数如 `?v=...`。
4. 旧的错误 `.jpg` 可保留不引用，或删除以免混淆；不要再生成上采样假清晰 JPG。

## 对用户说明

若曾误用聊天压缩图，应明确说明：模糊是因为附件被压缩，不是 Figma 2× 导出本身有问题；已改为使用桌面原 PNG。
